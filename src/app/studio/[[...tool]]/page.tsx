import { StudioClientGateway } from "./studio-gateway";

/**
 * Sanity Studio — treść marketingowa (m.in. zdjęcie produktu w hero).
 */
export default function StudioPage() {
	return <StudioClientGateway />;
}
